import java.util.ArrayList;
class Test
{
  public static void main(String args[])
  {
  ArrayList<Integer> al = new ArrayList<Integer>(); // putting a number inside () will make the array static
    al.add(10);
    al.add(20);
    System.out.print(al);
  }
}
